{
  "name": "@argent/mcp",
  "version": "0.1.0",
  "type": "module",
  "description": "Argent stdio MCP server.",
  "bin": {
    "argent-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@argent/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^4.2.0"
  }
}
